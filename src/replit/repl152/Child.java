package replit.repl152;

class Child extends Parent{
    Child(String city){
        super(city);//вызывает(обращаеться конструктор родителя в которм есть sout
        //sout(city) --так выведет в консоль ит первый и второй конструктор
    }
}